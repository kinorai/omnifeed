# GoReleaser config — https://goreleaser.com
# Uses v2.16+ syntax. dockers_v2 graduated out of experimental in v2.16.
#
# Tag-driven release:
#   git tag v0.1.0 && git push --tags
# CI workflow then runs:
#   goreleaser release --clean
version: 2

project_name: omnifeed

before:
  hooks:
    - go mod tidy
    - go mod verify

builds:
  - id: omnifeed
    main: ./cmd/omnifeed
    binary: omnifeed
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X github.com/kinorai/omnifeed/internal/version.Version={{ .Version }}
    mod_timestamp: "{{ .CommitTimestamp }}"

archives:
  - id: default
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: ["zip"]
    files:
      - LICENSE
      - README.md
      - CHANGELOG.md

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

# Multi-arch Docker image, published to Docker Hub.
# Requires DOCKERHUB_USERNAME + DOCKERHUB_TOKEN in CI; goreleaser does the
# `docker buildx` manifest build using the pre-built binaries above.
dockers_v2:
  - id: omnifeed
    ids:
      - omnifeed
    dockerfile: goreleaser.Dockerfile
    images:
      - kinorai/omnifeed
    tags:
      - "{{ .Version }}"
      - "v{{ .Version }}"
      - "{{ if not .IsNightly }}latest{{ end }}"
      - "{{ .Major }}"
      - "{{ .Major }}.{{ .Minor }}"
    platforms:
      - linux/amd64
      - linux/arm64
    labels:
      org.opencontainers.image.title: "{{ .ProjectName }}"
      org.opencontainers.image.description: "Self-hosted web search (SearXNG) + LLM-friendly crawling with a dedicated Reddit engine — MCP server, Open WebUI compatible"
      org.opencontainers.image.url: "https://github.com/kinorai/omnifeed"
      org.opencontainers.image.source: "https://github.com/kinorai/omnifeed"
      org.opencontainers.image.version: "{{ .Version }}"
      org.opencontainers.image.revision: "{{ .FullCommit }}"
      org.opencontainers.image.created: "{{ .Date }}"
      org.opencontainers.image.licenses: "MIT"

sboms:
  - id: archive
    artifacts: archive
  - id: source
    artifacts: source

source:
  enabled: true
  name_template: "{{ .ProjectName }}_{{ .Version }}_source"

snapshot:
  # Use '-' not '+' before the commit: this version is used as a Docker tag in
  # the snapshot build (dockers_v2), and '+' (SemVer build metadata) is an
  # invalid Docker tag character — `docker buildx build` rejects it with
  # "invalid reference format". '-dev-<sha>' is both a valid SemVer prerelease
  # and a valid Docker tag. Real releases use the clean git-tag version, so this
  # only affects snapshot/PR-validation builds.
  version_template: "{{ incpatch .Version }}-dev-{{ .ShortCommit }}"

changelog:
  use: git
  sort: asc
  groups:
    - title: "🛠 Breaking changes"
      regexp: '^.*?(feat|fix|refactor)(\(.+\))??!:.+$'
      order: 0
    - title: "🎉 Features"
      regexp: '^.*?feat(\(.+\))??:.+$'
      order: 1
    - title: "🐛 Bug fixes"
      regexp: '^.*?fix(\(.+\))??:.+$'
      order: 2
    - title: "♻️ Refactor"
      regexp: '^.*?refactor(\(.+\))??:.+$'
      order: 3
    - title: "⚡️ Performance"
      regexp: '^.*?perf(\(.+\))??:.+$'
      order: 4
    - title: "📄 Documentation"
      regexp: '^.*?docs(\(.+\))??:.+$'
      order: 5
    - title: "✅ Tests"
      regexp: '^.*?test(\(.+\))??:.+$'
      order: 6
    - title: "⬆️ Dependencies"
      regexp: '^.*?(chore|build)\(deps\)??:.+$'
      order: 7
    - title: "Other changes"
      order: 999
  filters:
    exclude:
      - '^chore: bump'
      - '^Merge pull request'
      - '^Merge branch'

release:
  github:
    owner: kinorai
    name: omnifeed
  draft: false
  prerelease: auto
  mode: replace
  header: |
    ## omnifeed {{ .Tag }}

    Self-hosted web search (SearXNG) + LLM-friendly crawling with a dedicated Reddit engine — MCP server, Open WebUI compatible.
  footer: |
    ## Docker

    ```
    docker pull kinorai/omnifeed:{{ .Version }}
    ```

    ## Verify

    The `checksums.txt` file contains SHA-256 hashes for every artifact in this release.
